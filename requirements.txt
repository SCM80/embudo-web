# Balanzia Invest — dependencias (todo gratuito, sin API keys de pago)
# Versiones FIJADAS a propósito: evita que al reinstalar cambie el JS de Streamlit
# y el navegador falle con "Failed to fetch dynamically imported module".
streamlit==1.58.0
streamlit-autorefresh==1.0.1   # auto-refresco "casi real" del dashboard
pandas>=2.0
numpy>=1.24
plotly>=5.20
yfinance>=0.2.40        # Datos de mercado (Yahoo Finance, sin API key)
lxml>=4.9               # leer componentes de índices desde Wikipedia (universos dinámicos)
vaderSentiment>=3.3.2   # Sentimiento local, sin API
requests>=2.31
